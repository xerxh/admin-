tinymce.PluginManager.add("lineheightnction(t, e) {
	t.addButton("lineheight", function(){
		var items = [];
		items.push({text:'100%',value:'100itemitems.push({text:'150%',value:'150itemitems.push({text:'200%',value:'200itemitems.push({text:'250%',value:'250eturn {
			type : 'listbox',
			text : "行间距",
			values : items,
			onselect:function(e){
				if(e.control.settings.value){
					var ed = tinymce.activeEditor;
					var node = ed.selection.getNode();
					var text = ed.selection.getContent({'format':'text'});
					if (node && text) {
						ed.dom.setStyle(node,'lineHeight',e.control.settings.value} else {
						alert('请选择需要更改的内容');
					}
				}
			}
		};
	})
});